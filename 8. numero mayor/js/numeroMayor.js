/*
 * Numero mayor
 * Autor:Santiago Gonzalez Vasquez
 * Fecha 31 de marzo de 2024
 * version 1.0
 */


function mayor(anumUno, anumDos){
    let numUno = anumUno
    let numDos = anumDos
    if(numUno > numDos)
    return "el numero UNO es mayor (1): " + numUno;
    else return "el numero DOS es mayor (1): " + numDos;
}

const mayorExpresion = function(bnumUno, bnumDos){
    let numUno = bnumUno
    let numDos = bnumDos
    if(numUno > numDos)
    return "el numero UNO es mayor (2): " + bnumUno;
    else return "el numero DOS es mayor (2): " + bnumDos;
}