/*
 * Suma
 * Autor:Santiago Gonzalez Vasquez
 * Fecha 30 de marzo de 2024
 * version 1.0
 */

function suma(pnumUno,pnumDos){
    let numUno = pnumUno 
    let numDos = pnumDos 
    return parseInt(numUno)+parseInt(numDos);
}

const sumarExpresion = function(pnumUno, pnumDos){
    let numUno = pnumUno 
    let numDos = pnumDos 
    return parseInt(numUno)+parseInt(numDos);
}
